This folder holds docker compose ".yml" and ".env" files to quickly setup stacks


to run use:
	docker compose up

Both the ".yml" and ".env" must be in the in the same folder to run, otherwise you will have to specify file locations.
